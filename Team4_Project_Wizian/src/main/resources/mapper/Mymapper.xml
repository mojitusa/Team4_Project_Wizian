<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Mymapper">
	<select id="myStud" resultType="int" parameterType="int">
		SELECT
		SUM(count) AS total_count
		FROM (
		SELECT COUNT(*) AS count
		FROM CSL_BOARD
		WHERE SUBSTRING(STUD_NO, 1, 2) = #{secd}

		UNION ALL

		SELECT COUNT(*) AS
		count
		FROM PRGRM_APP
		WHERE SUBSTRING(STUD_NO, 1, 2) = #{secd}

		UNION ALL

		SELECT COUNT(*) AS count
		FROM PROFESSER_CONSULTATION
		WHERE
		SUBSTRING(STUD_NO, 1, 2) = #{secd}

		UNION ALL

		SELECT COUNT(*) AS count
		FROM CSL_APPLY
		WHERE SUBSTRING(STUD_NO, 1, 2) = #{secd}
		) AS cc;
	</select>

	<update id="updatePrivacy" parameterType="map">
		UPDATE STUD_INFO AS si
		INNER JOIN USERS AS u ON si.stud_no = u.user_no
		SET si.MBR_TELNO =
		#{phoneNumber},
		u.email = #{email}
		WHERE si.stud_no = #{studNo}
	</update>


	<select id="getAllBoard" parameterType="map" resultType="String">
		SELECT *
FROM PRGRM_APP pa
JOIN PROFESSER_CONSULTATION pc ON pa.stud_no = pc.stud_no
JOIN CSL_APPLY ca ON pa.stud_no = ca.stud_no
WHERE pa.stud_no = #{userNo};
	</select>
</mapper>
